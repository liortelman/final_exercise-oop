/**
 * Enum class. change the types of updates that can be for a flight (departure time change, cancellation, promotion).
 */
public enum UpdateType {
    DEPARTURE_TIME_CHANGE,
    CANCELLATION,
    PROMOTION
}